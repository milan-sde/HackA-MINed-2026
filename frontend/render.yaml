services:
  - type: web
    name: smartcontainer-frontend
    env: static
    buildCommand: npm install && npm run build:render
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: "20"